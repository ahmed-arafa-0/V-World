# Release corrections — 2026-09-24

Ahmed authorized the existing working-tree application release to the existing Render FREE service. This release includes the previously implemented island, first journey, world locations, media gateway, birthday event and supporting source, followed by the ten reported corrections. No paid hosting, new service, Firebase deployment or real-owner reset is included.

## Corrections and live findings

- **Map:** the avatar's top-edge transform caused the oversized, low placement. It now uses measured sprite feet and dedicated positions in the 1792×2128 island plane, with a smaller bounded size and clearance from controls. Opening the map carries the immediately preceding scene location, including Church exterior scene transitions. Sea explicitly opens `beach_steps`, bypassing the stale saved `church_focus` presentation checkpoint. Failed/denied server travel no longer navigates locally. Server access checks and reward rules remain authoritative.
- **Walkman:** the live catalog contained TUL8TE and three unconfigured placeholder rows, but no Henry Moodie row. Read-only traversal of the approved Drive root found exactly one `Henry Moodie you were there for me.mp3` (audio/mpeg, 3,191,275 bytes). Registered its actual discovered file, without uploading or inventing a Drive ID, as `asset_audio_song_henry_moodie_001` / `song_henry_moodie_001`. No matching asset/song already existed. Added the supplied TUL8TE artist. Both enabled, unrestricted existing supplied songs appear and play through the authenticated gateway. Placeholder birthday songs were not enabled or rescheduled.
- **Church audio:** removed the Audio object, playback effects, prompts, preference controls and fade-out code from interior and candle corner. The backend no longer reads or returns Church audio references (empty hymns/null reading field retained for contract compatibility). Exterior bell behavior is unchanged; Walkman silence and companion exclusion remain. Drive files were not deleted.
- **Church content:** actual date was September 24 in Africa/Cairo. The current story, verse and six questions were already enabled and approved, authored only in Arabic. The English-only fallback discarded them for other UI languages. Requested locale → English → authored Arabic now resolves content and quiz answers consistently and reports Arabic/RTL correctly. No Church schedule, approval or enabled cells changed.
- **Mailbox:** read-only inspection of the ordinary review confirmed the welcome letter and September 23/24 messages already existed as archived deliveries. All 1,011 localized message rows map locally from the real recipient to the review identity; live recipients are unchanged. Existing unread/archive lists are preserved. Opening the mailbox refreshes it, and stale initial GET responses cannot overwrite a newer delivery response. The unread badge is attached to the mailbox button rather than stretching across the scene. `admin_ahmed` displays as localized Arafa/عرفة using the existing Sheet-first UI text convention.
- **Icons:** only three startup derivatives of the approved sunflower icon are published: favicon.ico, favicon-32.png and apple-touch-icon.png. They load before login; the private media gateway remains authenticated.
- **Confetti:** a fixed pool of 40 CSS particles starts at countdown zero and remains throughout celebration steps. Countdown-only replay remains open at zero until explicitly closed. Reduced motion shows a static glow. Closing/unmounting removes all particles; no growth loop or background timer exists, and pointer events pass through.
- **Cat hint:** the button inherited the full sprite width through its inline positioning object. It now inherits position only and stays 36×36. Birthday companion artwork has no hint oval.
- **Candles:** three SVG viewports crop the existing padded body/flame assets without publishing them publicly. Bases sit in a rear row on the cake; wicks/flames align at desktop and portrait sizes. Extinguishing removes only flames. The single action's label is plural in all five locales, including narrowly updated existing Sheet UI rows.
- **Completion:** successful completion now closes to the world immediately; the thank-you panel is removed. A failed save remains retryable. Once-only gifts and both replay entry points remain.

## Verification

- Focused web suite: **55 tests passed** across birthday, review corrections, Cottage/Museum views, SceneJourney and candle recovery.
- Focused backend suite: **100 tests passed** across Church/journey, world locations, birthday API, production clock boundary and reward lockdown. Added Arabic-only fallback/quiz coverage.
- Real Sheet/Drive browser verification: desktop 1280×900 and portrait 390×844, newly created isolated players. Both actual audio files played; all seven map destinations opened; Sea showed the beach; current map location matched the prior view. Navigation changed neither reward rows nor story-completion fields. Visible verse, loaded illustrated story and playable quiz used the real Cairo date. Three currently eligible letters were verified, including archive and localized sender. The fourth mailbox message in final QA results is the birthday gift claimed only by these disposable players under their event-scoped clock.
- Birthday: real loaded assets, three visible flames then three remaining bodies, persistent 40-particle confetti through gifts, direct close after saved completion, countdown-only replay with identical reward/state records. Final presentation checks cover control clearance and reduced motion.
- Root build, typecheck and lint; frontend credential-boundary scan. Clean staged checkout successfully ran the exact production sequence: `npm ci --include=dev`, `npm run build`, `npm prune --omit=dev` on Node 22.
- Pruned production runtime smoke: HTML/icons 200; production health and Sheets reachable; unauthenticated world/private media denied; development map/test-clock routes 404. Production entry point explicitly enforces production environment and secure cookies even if host NODE_ENV is omitted.
- No ordinary review reset or real-owner gameplay mutation. Only disposable QA player rows changed during gameplay verification. Catalog changes are the narrow song/UI corrections above.

Private evidence remains excluded in `test-results/release/`: catalog-before snapshots, catalog-patch-before snapshot, live-results.json, desktop/portrait screenshots, build/test logs, clean checkout and QA session manifests. None is part of the release commit.

## Release review and limitations

Reviewed the complete staged file inventory and release diff structure, including auth middleware, public-media allowlist, production entry point, development guards, reward paths, config loader and package commands. Required `scripts/lib/phase2-fixture.mjs` and authored welcome seed input are preserved. Raw media packs, secrets/.env, private snapshots, manifests, logs, profiles and new scratch screenshots are excluded. Existing unrelated `.firebaserc` and `firebase.json` edits remain unstaged.

The final production dependency prune reports 11 moderate npm advisories. No dependency upgrades outside the requested release fixes were attempted. Browser QA also observed the existing map icon's unsupported-MIME fallback and missing optional beach ambience; the map button remains usable through its existing icon fallback. The requested island/story/birthday images and both songs loaded successfully.

Repository-wide formatting retains the prior **60-file baseline**. Touched correction files pass targeted formatting. `git diff --check` reports four existing intentional Markdown hard-break lines in three documents. No broad unrelated formatting was performed.

Git remote is `https://github.com/ahmed-arafa-0/V-World.git`, branch `main`, remote base `d2489ce6a3a455bf0748099980f32c935aefb6c2`; authenticated push dry-run succeeded. No force push is used.

**Deployment blocked:** no Render connector/credential or connected browser is available; both in-app browser and Chrome report unavailable. The service URL/name and configured build/start/branch could not be verified. No actual push or deployment has been triggered, because automatic deploy settings must be checked first. The local release is prepared for the existing FREE service. Remaining handoff: provide access to that existing Render service dashboard (or its URL and verified configuration) so the connected repository/main, Node 22, FREE instance, build command, start command and existing Secret File can be confirmed before push. Preserve the Secret File/environment; do not create a service or change billing.

Required existing-service commands: build `npm ci --include=dev && npm run build && npm prune --omit=dev`; start `npm run start --workspace=apps/functions`. Once deployed, run the same production smoke against its public HTTPS URL and verify the Gate without changing owner progress.

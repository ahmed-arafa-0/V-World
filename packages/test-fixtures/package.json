{
  "name": "@veoullas-world/test-fixtures",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic mock Google Sheets workbook fixtures for M01+ tests (no real credentials or live data)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@veoullas-world/sheet-schema": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}

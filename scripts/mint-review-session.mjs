#!/usr/bin/env node
// Ordinary review now uses the real Gate in a dedicated profile and the manifest-selected identity.
// Do not mint or print bypass cookies for a hard-coded historical player.
console.log('Open http://127.0.0.1:5051 with: node scripts/open-fresh-review.mjs');
console.log('If stopped: node scripts/serve-fresh-review.mjs --open');
